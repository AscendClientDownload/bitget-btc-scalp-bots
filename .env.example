# No credentials are required or used yet — all data endpoints used by this
# project are Bitget's public market-data API (no auth). This file is a
# placeholder for when/if authenticated live trading is added later.
# BITGET_API_KEY=
# BITGET_API_SECRET=
# BITGET_API_PASSPHRASE=

# Local dashboard server port
DASHBOARD_PORT=5000
